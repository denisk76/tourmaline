spring:
  kafka:
    streams:
      application-id: cityBps
    bootstrap-servers: localhost:9092
  #    consumer:
#      auto-offset-reset: earliest
#      group-id: groupid
  application:
    name: tourmaline